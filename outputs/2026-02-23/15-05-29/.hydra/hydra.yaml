hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.mode=RUN
    task:
    - data.train_files=./data/train.parquet
    - data.val_files=./data/dev.parquet
    - data.train_batch_size=32
    - data.max_prompt_length=30767
    - data.max_response_length=2000
    - +data.max_model_len=32768
    - +data.data_writing_path=./cache/task_queue/
    - actor_rollout_ref.model.path=Qwen/Qwen2.5-7B-Instruct
    - actor_rollout_ref.model.use_remove_padding=true
    - actor_rollout_ref.actor.optim.lr=1e-6
    - actor_rollout_ref.actor.ppo_mini_batch_size=512
    - actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=2
    - actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=2
    - actor_rollout_ref.rollout.tensor_model_parallel_size=1
    - actor_rollout_ref.rollout.gpu_memory_utilization=0.65
    - actor_rollout_ref.rollout.max_num_batched_tokens=32768
    - actor_rollout_ref.rollout.max_model_len=32768
    - actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=2
    - actor_rollout_ref.actor.use_kl_loss=true
    - actor_rollout_ref.actor.use_dynamic_bsz=true
    - actor_rollout_ref.actor.fsdp_config.param_offload=true
    - actor_rollout_ref.actor.fsdp_config.optimizer_offload=true
    - actor_rollout_ref.ref.fsdp_config.param_offload=true
    - actor_rollout_ref.actor.ppo_max_token_len_per_gpu=12288
    - actor_rollout_ref.actor.ulysses_sequence_parallel_size=4
    - actor_rollout_ref.rollout.temperature=1.0
    - critic.optim.lr=1e-5
    - critic.model.path=Qwen/Qwen2.5-7B-Instruct
    - critic.ppo_micro_batch_size_per_gpu=2
    - algorithm.gamma=1.0
    - +algorithm.info_gain_type=log_prob_diff
    - +algorithm.info_gain_norm_mode=separate
    - +algorithm.use_vectorized_gt_logprob=false
    - +algorithm.use_curriculum=false
    - +algorithm.curriculum_f1_init=0.5
    - +algorithm.curriculum_f1_final=1.0
    - +algorithm.curriculum_ig_init=1.0
    - +algorithm.curriculum_ig_final=0.5
    - algorithm.kl_ctrl.kl_coef=0.001
    - trainer.logger=[console,wandb]
    - trainer.project_name=Search
    - trainer.experiment_name=IGPO-Qwen2.5-7B-Instruct
    - trainer.val_before_train=false
    - trainer.default_hdfs_dir=null
    - trainer.n_gpus_per_node=8
    - trainer.nnodes=1
    - trainer.save_freq=1
    - trainer.test_freq=1
    - trainer.validation_data_dir=./eval_logs/Search/IGPO-Qwen2.5-7B-Instruct
    - trainer.default_local_dir=./outputs/Search/IGPO-Qwen2.5-7B-Instruct
    - agent_grpo.n=16
    - max_turns=10
    - search_engine=online_search
    - codeact_env_disabled=true
    - trainer.total_epochs=1
  job:
    name: main_ppo
    chdir: null
    override_dirname: +algorithm.curriculum_f1_final=1.0,+algorithm.curriculum_f1_init=0.5,+algorithm.curriculum_ig_final=0.5,+algorithm.curriculum_ig_init=1.0,+algorithm.info_gain_norm_mode=separate,+algorithm.info_gain_type=log_prob_diff,+algorithm.use_curriculum=false,+algorithm.use_vectorized_gt_logprob=false,+data.data_writing_path=./cache/task_queue/,+data.max_model_len=32768,actor_rollout_ref.actor.fsdp_config.optimizer_offload=true,actor_rollout_ref.actor.fsdp_config.param_offload=true,actor_rollout_ref.actor.optim.lr=1e-6,actor_rollout_ref.actor.ppo_max_token_len_per_gpu=12288,actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=2,actor_rollout_ref.actor.ppo_mini_batch_size=512,actor_rollout_ref.actor.ulysses_sequence_parallel_size=4,actor_rollout_ref.actor.use_dynamic_bsz=true,actor_rollout_ref.actor.use_kl_loss=true,actor_rollout_ref.model.path=Qwen/Qwen2.5-7B-Instruct,actor_rollout_ref.model.use_remove_padding=true,actor_rollout_ref.ref.fsdp_config.param_offload=true,actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=2,actor_rollout_ref.rollout.gpu_memory_utilization=0.65,actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=2,actor_rollout_ref.rollout.max_model_len=32768,actor_rollout_ref.rollout.max_num_batched_tokens=32768,actor_rollout_ref.rollout.temperature=1.0,actor_rollout_ref.rollout.tensor_model_parallel_size=1,agent_grpo.n=16,algorithm.gamma=1.0,algorithm.kl_ctrl.kl_coef=0.001,codeact_env_disabled=true,critic.model.path=Qwen/Qwen2.5-7B-Instruct,critic.optim.lr=1e-5,critic.ppo_micro_batch_size_per_gpu=2,data.max_prompt_length=30767,data.max_response_length=2000,data.train_batch_size=32,data.train_files=./data/train.parquet,data.val_files=./data/dev.parquet,max_turns=10,search_engine=online_search,trainer.default_hdfs_dir=null,trainer.default_local_dir=./outputs/Search/IGPO-Qwen2.5-7B-Instruct,trainer.experiment_name=IGPO-Qwen2.5-7B-Instruct,trainer.logger=[console,wandb],trainer.n_gpus_per_node=8,trainer.nnodes=1,trainer.project_name=Search,trainer.save_freq=1,trainer.test_freq=1,trainer.total_epochs=1,trainer.val_before_train=false,trainer.validation_data_dir=./eval_logs/Search/IGPO-Qwen2.5-7B-Instruct
    id: ???
    num: ???
    config_name: ppo_trainer
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /root/IGPO
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /root/IGPO/verl/trainer/config
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /root/IGPO/outputs/2026-02-23/15-05-29
    choices:
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
